---
phase: 01-state-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - detent-tools.cjs
  - test/run-tests.js
autonomous: true
requirements:
  - ENG-01
  - ENG-02
  - ENG-03

must_haves:
  truths:
    - "Running `node detent-tools.cjs` with no args prints usage and exits 0"
    - "state-write atomically updates .detent/state.json with pipeline_stage, current_unit, iteration_count, reentry_depth fields"
    - "state-read prints current state.json as JSON to stdout"
    - "config-write atomically updates .detent/config.json with mode, model_budget, locale, pipeline_stages, unit_granularity, language"
    - "config-read prints current config.json as JSON to stdout"
    - "setup creates .detent/ directory tree with default state.json and config.json"
    - "state.json includes reentry_depth field initialized to 0 from day one"
    - "setup accepts --mode, --budget, --locale, --granularity flags and applies them to initial config.json"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with write-file-atomic dependency"
      contains: "write-file-atomic"
    - path: "detent-tools.cjs"
      provides: "CLI tool — single entry point for all state mutations"
      min_lines: 150
      exports: "main() with command dispatch table"
    - path: "test/run-tests.js"
      provides: "Automated test suite for CLI commands"
      min_lines: 50
  key_links:
    - from: "detent-tools.cjs"
      to: "write-file-atomic"
      via: "require('write-file-atomic').sync"
      pattern: "require\\('write-file-atomic'\\)\\.sync"
    - from: "detent-tools.cjs"
      to: ".detent/state.json"
      via: "writeFileAtomicSync for all writes"
      pattern: "writeFileAtomicSync"
    - from: "test/run-tests.js"
      to: "detent-tools.cjs"
      via: "child_process.execSync invocations"
      pattern: "execSync.*detent-tools\\.cjs"
---

<objective>
Build the detent-tools.cjs CLI tool with all five commands (setup, state-read, state-write, config-read, config-write), the project package.json, and an automated test suite.

Purpose: This is the foundation layer — every downstream skill and agent calls detent-tools.cjs for state mutations. Nothing else works without this.
Output: Working CLI tool, installed dependency, passing test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-state-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and install write-file-atomic</name>
  <files>package.json</files>
  <read_first>
    .planning/phases/01-state-infrastructure/01-RESEARCH.md
  </read_first>
  <action>
    Create package.json at the project root with these exact contents:

    ```json
    {
      "name": "detent",
      "version": "0.1.0",
      "description": "Control-theory harness framework for multi-agent orchestration in Claude Code",
      "type": "commonjs",
      "main": "detent-tools.cjs",
      "scripts": {
        "test": "node test/run-tests.js"
      },
      "license": "UNLICENSED",
      "private": true
    }
    ```

    Then run `npm install write-file-atomic@^7.0.1` to install the single external dependency.

    CRITICAL: `"type": "commonjs"` must be set explicitly to prevent any future ESM confusion (per Pitfall 4 in RESEARCH.md).

    Do NOT add any other dependencies. Phase 1 needs exactly one: write-file-atomic.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/Detent && node -e "const p = require('./package.json'); const assert = require('assert'); assert.strictEqual(p.type, 'commonjs'); assert.strictEqual(p.name, 'detent'); console.log('package.json OK')" && node -e "require('write-file-atomic'); console.log('write-file-atomic installed OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"type": "commonjs"`
    - package.json contains `"name": "detent"`
    - package.json contains `"test": "node test/run-tests.js"` in scripts
    - `require('write-file-atomic')` resolves without error
    - node_modules/write-file-atomic/ directory exists
  </acceptance_criteria>
  <done>package.json exists with type commonjs, write-file-atomic@^7.0.1 installed and requireable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build detent-tools.cjs CLI with all commands</name>
  <files>detent-tools.cjs, test/run-tests.js</files>
  <read_first>
    .planning/phases/01-state-infrastructure/01-RESEARCH.md
    CLAUDE.md
  </read_first>
  <behavior>
    - Test: `node detent-tools.cjs` with no args prints "Usage:" to stdout and exits with code 0
    - Test: `node detent-tools.cjs setup --dir /tmp/detent-test-xxx` creates .detent/, .detent/state.json, .detent/config.json, .detent/truth-surface/, .detent/raw/, .detent/logs/
    - Test: state.json created by setup contains schema_version=1, pipeline_stage="idle", current_unit=null, iteration_count=0, reentry_depth=0, last_updated (ISO string), session_id=null
    - Test: config.json created by setup contains schema_version=1, mode="supervised", model_budget="balanced", locale="en", pipeline_stages (all true), unit_granularity="standard", language="en"
    - Test: `node detent-tools.cjs setup --dir ... --mode autonomous --budget quality --locale zh-CN --granularity fine` creates config.json with mode="autonomous", model_budget="quality", locale="zh-CN", unit_granularity="fine"
    - Test: `node detent-tools.cjs state-write --dir ... --pipeline_stage discovery` updates pipeline_stage to "discovery" in state.json
    - Test: `node detent-tools.cjs state-read --dir ...` outputs current state.json content as JSON to stdout
    - Test: `node detent-tools.cjs config-write --dir ... --mode autonomous` updates mode to "autonomous" in config.json
    - Test: `node detent-tools.cjs config-read --dir ...` outputs current config.json content as JSON to stdout
    - Test: `node detent-tools.cjs unknown-command` exits with code 1 and writes error to stderr
    - Test: state-write updates last_updated timestamp
    - Test: state-write with multiple fields (--pipeline_stage coding --iteration_count 3) updates both
  </behavior>
  <action>
    Create test/run-tests.js FIRST (RED phase), then implement detent-tools.cjs (GREEN phase).

    **test/run-tests.js** must:
    - Use Node.js built-in `assert` and `child_process.execSync`
    - Create a unique temp directory per test run via `fs.mkdtempSync(path.join(os.tmpdir(), 'detent-test-'))`
    - Clean up the temp directory after all tests
    - Run each test by spawning `node detent-tools.cjs <command> --dir <tmpdir>` as a subprocess
    - Parse stdout as JSON where applicable
    - Assert exact field values against expected schemas
    - Include a test that setup with `--mode autonomous --budget quality --locale zh-CN --granularity fine` produces a config.json with those values (not defaults)
    - Exit 0 on all pass, exit 1 on any failure with descriptive output

    **detent-tools.cjs** must implement:

    1. **Argument parsing:** `process.argv.slice(2)`, first positional = command. Named args parsed as `--key value` pairs. Support `--dir <path>` on all commands (default: `process.cwd()`).

    2. **Command: (no args)** — Print usage string to stdout listing all commands, exit 0.

    3. **Command: setup** — Create `.detent/` directory tree and initialize JSON files:
       - Directories: `.detent/`, `.detent/truth-surface/`, `.detent/raw/`, `.detent/logs/`
       - Accept optional flags: `--mode <val>`, `--budget <val>`, `--locale <val>`, `--granularity <val>`.
         These override the defaults in config.json when provided:
         - `--mode` sets `config.mode` (default: "supervised")
         - `--budget` sets `config.model_budget` (default: "balanced")
         - `--locale` sets `config.locale` AND `config.language` (default: "en")
         - `--granularity` sets `config.unit_granularity` (default: "standard")
       - `.detent/state.json` with default schema:
         ```json
         {
           "schema_version": 1,
           "pipeline_stage": "idle",
           "current_unit": null,
           "iteration_count": 0,
           "reentry_depth": 0,
           "last_updated": "<ISO 8601 now>",
           "session_id": null
         }
         ```
       - `.detent/config.json` with schema (values reflect flag overrides or defaults):
         ```json
         {
           "schema_version": 1,
           "mode": "<--mode or 'supervised'>",
           "model_budget": "<--budget or 'balanced'>",
           "locale": "<--locale or 'en'>",
           "pipeline_stages": {
             "discovery": true,
             "planning": true,
             "coding": true,
             "verification": true,
             "achieve": true
           },
           "unit_granularity": "<--granularity or 'standard'>",
           "language": "<--locale or 'en'>"
         }
         ```
       - Use `fs.mkdirSync(dir, { recursive: true })` for directories
       - Use `writeFileAtomicSync` for JSON files
       - If `.detent/` already exists, overwrite files (idempotent)
       - Print JSON result `{"ok": true, "dir": "<abs path>"}` to stdout

    4. **Command: state-read** — Read `.detent/state.json`, print to stdout as JSON. Exit 1 if file missing.

    5. **Command: state-write** — Read current state.json, merge provided `--field value` pairs, update `last_updated` to current ISO timestamp, write back atomically. Numeric fields (`iteration_count`, `reentry_depth`) parsed as integers via `parseInt`. Exit 1 if .detent/ not initialized.

    6. **Command: config-read** — Read `.detent/config.json`, print to stdout as JSON. Exit 1 if file missing.

    7. **Command: config-write** — Read current config.json, merge provided `--field value` pairs, write back atomically. For `pipeline_stages`, accept `--pipeline_stages.discovery false` dot-notation to set nested fields. Boolean strings `"true"`/`"false"` converted to actual booleans. Exit 1 if .detent/ not initialized.

    8. **Unknown command** — Write `Unknown command: <cmd>\n` to stderr, exit 1.

    Critical implementation details:
    - `'use strict';` at top of file
    - `const writeFileAtomicSync = require('write-file-atomic').sync;` — sync variant only
    - All JSON writes use `writeFileAtomicSync(path, JSON.stringify(data, null, 2) + '\n', { encoding: 'utf8' })`
    - All JSON reads use `JSON.parse(fs.readFileSync(path, 'utf8'))`
    - No async/await anywhere in the file
    - No ESM syntax (no import/export)
    - Target: under 300 lines for detent-tools.cjs
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/Detent && node test/run-tests.js</automated>
  </verify>
  <acceptance_criteria>
    - detent-tools.cjs starts with `'use strict';`
    - detent-tools.cjs contains `require('write-file-atomic').sync`
    - detent-tools.cjs contains `switch` or equivalent dispatch for commands: setup, state-read, state-write, config-read, config-write
    - `node detent-tools.cjs` exits 0 and stdout contains "Usage:"
    - `node detent-tools.cjs setup --dir /tmp/test` creates .detent/state.json with reentry_depth field
    - `node detent-tools.cjs setup --dir /tmp/test` creates .detent/config.json with pipeline_stages object
    - `node detent-tools.cjs setup --dir /tmp/test --mode autonomous --budget quality` creates config.json with mode="autonomous" and model_budget="quality"
    - `node detent-tools.cjs state-write --dir /tmp/test --pipeline_stage discovery` updates state.json pipeline_stage to "discovery"
    - `node detent-tools.cjs state-read --dir /tmp/test` outputs JSON with pipeline_stage field
    - test/run-tests.js exists and contains assert statements
    - `node test/run-tests.js` exits 0
  </acceptance_criteria>
  <done>
    All 5 CLI commands work correctly. Test suite passes with 0 failures. state.json includes all day-one fields (pipeline_stage, current_unit, iteration_count, reentry_depth). config.json includes all required fields (mode, model_budget, locale, pipeline_stages, unit_granularity, language). Setup command accepts --mode, --budget, --locale, --granularity flags to override defaults. All writes use writeFileAtomicSync.
  </done>
</task>

</tasks>

<verification>
1. `node detent-tools.cjs` prints usage and exits 0
2. `node test/run-tests.js` passes all tests
3. `node -e "require('write-file-atomic')"` succeeds
4. `grep -c 'writeFileAtomicSync' detent-tools.cjs` returns >= 2 (state + config writes)
5. `head -1 detent-tools.cjs` shows `'use strict';`
6. `node detent-tools.cjs setup --dir /tmp/detent-verify --mode autonomous && node -e "const c = require('fs').readFileSync('/tmp/detent-verify/.detent/config.json','utf8'); const j = JSON.parse(c); require('assert').strictEqual(j.mode, 'autonomous'); console.log('setup flags OK')"` — verifies setup flag passthrough
</verification>

<success_criteria>
- detent-tools.cjs exists with 5 working commands (setup, state-read, state-write, config-read, config-write)
- write-file-atomic installed and used for all JSON writes
- state.json schema has all day-one fields including reentry_depth
- config.json schema has all ENG-03 fields
- setup command accepts --mode, --budget, --locale, --granularity flags and applies to config.json
- test/run-tests.js passes with exit code 0
- No ESM syntax, no async code in the CLI
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-infrastructure/01-01-SUMMARY.md`
</output>
