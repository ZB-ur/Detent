---
phase: 03-truth-surface-and-planning-agents
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/d-critique.md
  - .claude/agents/g-red.md
  - .claude/agents/g-blue.md
  - .claude/agents/h-review.md
  - .claude/agents/j-compile.md
  - .detent/playbooks/stage-playbook.md
  - .detent/playbooks/subagent-protocol.md
  - .detent/playbooks/handoff-quality-bar.md
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05

must_haves:
  truths:
    - "D-Critique agent template exists with Read+Bash tools and instructions to attack requirements against truth surface"
    - "G-Red agent template exists with Read+Bash tools and instructions to produce genuine attack on prior output"
    - "G-Blue agent template exists with Read+Bash+Write tools and instructions to defend against G-Red's attack"
    - "H-Review agent template exists with Read+Bash+Write tools and outputs machine-parseable JSON verdict"
    - "J-Compile agent template exists with Read+Write tools and generates executable code handoff"
    - "Three playbook files exist in .detent/playbooks/ with quality standards migrated from ECL principles"
  artifacts:
    - path: ".claude/agents/d-critique.md"
      provides: "D-Critique agent template"
      contains: "tools: Read, Bash"
    - path: ".claude/agents/g-red.md"
      provides: "G-Red agent template"
      contains: "tools: Read, Bash"
    - path: ".claude/agents/g-blue.md"
      provides: "G-Blue agent template"
      contains: "tools: Read, Bash, Write"
    - path: ".claude/agents/h-review.md"
      provides: "H-Review agent template"
      contains: "verdict"
    - path: ".claude/agents/j-compile.md"
      provides: "J-Compile agent template"
      contains: "tools: Read, Write"
    - path: ".detent/playbooks/stage-playbook.md"
      provides: "Stage quality standards"
      contains: "Quality"
    - path: ".detent/playbooks/subagent-protocol.md"
      provides: "Subagent interaction rules"
      contains: "Protocol"
    - path: ".detent/playbooks/handoff-quality-bar.md"
      provides: "Handoff acceptance criteria"
      contains: "Quality Bar"
  key_links:
    - from: ".claude/agents/d-critique.md"
      to: ".detent/playbooks/subagent-protocol.md"
      via: "@ reference in agent body"
      pattern: "@.detent/playbooks/subagent-protocol.md"
    - from: ".claude/agents/h-review.md"
      to: ".detent/plan/h-review-verdict.json"
      via: "Write tool output"
      pattern: "h-review-verdict.json"
    - from: ".claude/agents/j-compile.md"
      to: ".detent/plan/handoff.md"
      via: "Write tool output"
      pattern: "handoff.md"
---

<objective>
Create five Claude Code agent templates (.claude/agents/*.md) for the adversarial planning pipeline and three playbook files (.detent/playbooks/) with quality standards migrated from ECL principles.

Purpose: Define the agent roles, tool permissions, prompt instructions, and quality standards that the /detent:plan skill will orchestrate in Plan 03.
Output: 5 agent template files, 3 playbook files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-truth-surface-and-planning-agents/03-CONTEXT.md
@.planning/phases/03-truth-surface-and-planning-agents/03-RESEARCH.md

<interfaces>
<!-- Agent frontmatter format (verified from Claude Code docs): -->
```yaml
---
name: <agent-name>
description: <one-line description>
tools: <comma-separated tool names: Read, Write, Edit, Bash, Glob, Grep>
model: inherit
maxTurns: <5-10>
---
```

<!-- Truth surface CLI commands (from Plan 01 — will exist by execution time): -->
```bash
node ./detent-tools.cjs truth-propose --dir . --id <ID> --file <file> --source-agent <agent> --rationale "<text>"
node ./detent-tools.cjs truth-freeze --dir . --id <ID> --file <file>
node ./detent-tools.cjs truth-read --dir . --file <file>
node ./detent-tools.cjs truth-update --dir . --id <ID> --file <file> --challenged-by <agent>
```

<!-- Agent output file conventions: -->
```
.detent/plan/d-critique-output.md   — D-Critique writes structured critique
.detent/plan/g-red-output.md        — G-Red writes attack points
.detent/plan/g-blue-output.md       — G-Blue writes defense points
.detent/plan/h-review-verdict.json  — H-Review writes JSON verdict
.detent/plan/handoff.md             — J-Compile writes code handoff document
```

<!-- Shared rules pattern (from .claude/skills/_shared/rules.md): -->
```
- NEVER use the Write tool on any file inside `.detent/` -- all writes go through `node ./detent-tools.cjs`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create five agent template files in .claude/agents/</name>
  <files>.claude/agents/d-critique.md, .claude/agents/g-red.md, .claude/agents/g-blue.md, .claude/agents/h-review.md, .claude/agents/j-compile.md</files>
  <read_first>
    - .planning/phases/03-truth-surface-and-planning-agents/03-RESEARCH.md (agent template examples, tool notes, frontmatter format)
    - .planning/phases/03-truth-surface-and-planning-agents/03-CONTEXT.md (D-05 through D-13 decisions)
    - .claude/skills/_shared/rules.md (single-mutation-point rules that agents must follow)
    - .claude/skills/detent-plan/SKILL.md (current placeholder — understand how the skill will invoke these agents)
  </read_first>
  <action>
Create `.claude/agents/` directory and five agent template files. Per D-05, D-06, D-07, D-08.

**d-critique.md** (per D-06: minimum privilege; per PLAN-01):
```yaml
---
name: d-critique
description: Critiques requirements against the truth surface, attacking false assumptions
tools: Read, Bash
model: inherit
maxTurns: 7
---
```
Body must include:
- Role definition: "You are D-Critique, the first stage of the Detent adversarial planning pipeline."
- `@.detent/playbooks/subagent-protocol.md` and `@.detent/playbooks/stage-playbook.md` references
- Input files section: read `.detent/truth-surface/frozen-decisions.md`, `.detent/truth-surface/constraint-ledger.md`, `.detent/truth-surface/domain-model.md`
- Task: identify at least one requirement that deserves challenge; check alignment with frozen decisions
- Truth surface mutations via Bash: `node ./detent-tools.cjs truth-propose --dir . --id <ID> --file frozen-decisions --source-agent d-critique --rationale "<text>"`
- Explicit prohibition: "Do NOT use the Write tool on `.detent/` files. Use Bash to call detent-tools.cjs for truth surface mutations."
- Output: write structured critique to `.detent/plan/d-critique-output.md` using Write tool (this file is in `.detent/plan/`, NOT `.detent/truth-surface/`, so Write is allowed here)
- Wait — D-Critique has tools `Read, Bash` only (no Write per D-06 minimum privilege). So D-Critique must write its output file via Bash: `cat > .detent/plan/d-critique-output.md << 'EOF' ... EOF`
- Output structure: each requirement examined, which challenged and why, proposed constraint IDs, open questions for G-Red

**g-red.md** (per PLAN-02, D-10: attack role):
```yaml
---
name: g-red
description: Attacks the D-Critique output — finds weaknesses in the critique and proposed constraints
tools: Read, Bash
model: inherit
maxTurns: 7
---
```
Body must include:
- Role: "You are G-Red, the adversarial attacker in the Detent planning pipeline."
- `@.detent/playbooks/subagent-protocol.md` reference
- Input: read `.detent/plan/d-critique-output.md` (D-Critique's output), `.detent/truth-surface/frozen-decisions.md`
- Task: produce genuine attacks against D-Critique's analysis — NOT agreement. Find at least one flaw, gap, or unconsidered risk.
- Can mark entries as challenged via: `node ./detent-tools.cjs truth-update --dir . --id <ID> --file frozen-decisions --challenged-by g-red`
- Output via Bash to `.detent/plan/g-red-output.md`: structured list of attack points with severity
- Explicit: "You MUST disagree with or attack at least one point. If D-Critique's analysis is genuinely flawless, attack the completeness — what did it miss?"

**g-blue.md** (per PLAN-02, D-10: defense role, reads Red's output):
```yaml
---
name: g-blue
description: Defends against G-Red's attack — provides targeted rebuttals and proposes resolutions
tools: Read, Bash, Write
model: inherit
maxTurns: 7
---
```
Body must include:
- Role: "You are G-Blue, the adversarial defender in the Detent planning pipeline."
- `@.detent/playbooks/subagent-protocol.md` reference
- Input: read `.detent/plan/g-red-output.md` (G-Red's attacks), `.detent/plan/d-critique-output.md`, `.detent/truth-surface/frozen-decisions.md`
- Task: for each attack point from G-Red, provide a targeted defense or acknowledge the weakness and propose a mitigation
- Can propose new constraints or mark entries as challenged via Bash
- Output: write to `.detent/plan/g-blue-output.md` (G-Blue has Write tool for output files in `.detent/plan/`)
- Explicit: "Do NOT use Write on `.detent/truth-surface/` files — use Bash for truth-propose/truth-update."

**h-review.md** (per PLAN-03, D-11):
```yaml
---
name: h-review
description: Reviews the planning debate and judges whether the plan is coding-ready
tools: Read, Bash, Write
model: inherit
maxTurns: 5
---
```
Body must include:
- Role: "You are H-Review, the coding-readiness judge in the Detent planning pipeline."
- `@.detent/playbooks/stage-playbook.md` and `@.detent/playbooks/handoff-quality-bar.md` references
- Input: read `.detent/plan/d-critique-output.md`, `.detent/plan/g-red-output.md`, `.detent/plan/g-blue-output.md`, `.detent/truth-surface/frozen-decisions.md`
- Task: judge whether the plan is ready for coding; assess if all frozen decisions are respected, if contradictions are resolved
- Output format (CRITICAL — per pitfall 2 from research): write EXACTLY this JSON structure to `.detent/plan/h-review-verdict.json` using Write tool:
```json
{
  "verdict": "approved",
  "reentry_stage": null,
  "reason": "Explanation here"
}
```
OR:
```json
{
  "verdict": "rejected",
  "reentry_stage": "D",
  "reason": "Explanation here"
}
```
- Valid values for `reentry_stage`: `"D"` or `"G"` when rejecting; `null` when approving
- Explicit: "Do not add any fields. Do not wrap in markdown code fences. Write ONLY valid JSON to the file."

**j-compile.md** (per PLAN-04):
```yaml
---
name: j-compile
description: Compiles the approved plan into an executable code handoff document
tools: Read, Write
model: inherit
maxTurns: 10
---
```
Body must include:
- Role: "You are J-Compile, the final stage of the Detent adversarial planning pipeline."
- `@.detent/playbooks/handoff-quality-bar.md` reference
- Input: read all prior outputs (d-critique, g-red, g-blue, h-review-verdict), `.detent/truth-surface/frozen-decisions.md`, `.detent/truth-surface/domain-model.md`
- Task: synthesize all planning debate into an executable code handoff document that a Coder agent can act on without further clarification
- Output: write to `.detent/plan/handoff.md` using Write tool
- Handoff structure must include: implementation units (ordered), for each unit: files to create/modify, acceptance criteria, frozen constraints that apply, specific technical decisions
- Explicit: "The Coder agent will implement from this document alone. Every ambiguity you leave is a defect."
  </action>
  <verify>
    <automated>test -f .claude/agents/d-critique.md && test -f .claude/agents/g-red.md && test -f .claude/agents/g-blue.md && test -f .claude/agents/h-review.md && test -f .claude/agents/j-compile.md && echo "ALL AGENTS EXIST" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/d-critique.md contains `name: d-critique` and `tools: Read, Bash` and `maxTurns: 7` and `truth-propose` and `d-critique-output.md`
    - .claude/agents/g-red.md contains `name: g-red` and `tools: Read, Bash` and `MUST disagree` and `g-red-output.md`
    - .claude/agents/g-blue.md contains `name: g-blue` and `tools: Read, Bash, Write` and `g-blue-output.md` and `g-red-output.md`
    - .claude/agents/h-review.md contains `name: h-review` and `tools: Read, Bash, Write` and `h-review-verdict.json` and `"verdict"` and `"reentry_stage"`
    - .claude/agents/j-compile.md contains `name: j-compile` and `tools: Read, Write` and `maxTurns: 10` and `handoff.md`
    - All five agent files contain `model: inherit`
    - d-critique.md contains `@.detent/playbooks/subagent-protocol.md`
    - h-review.md contains `@.detent/playbooks/handoff-quality-bar.md`
    - d-critique.md contains "Do NOT use the Write tool on"
    - g-blue.md contains "Do NOT use Write on"
  </acceptance_criteria>
  <done>All five agent templates exist with correct frontmatter, tool permissions follow minimum-privilege principle, output file conventions are specified, truth surface mutations route through detent-tools.cjs CLI, and H-Review's verdict format is explicitly templated as JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Create three playbook files in .detent/playbooks/</name>
  <files>.detent/playbooks/stage-playbook.md, .detent/playbooks/subagent-protocol.md, .detent/playbooks/handoff-quality-bar.md</files>
  <read_first>
    - .planning/phases/03-truth-surface-and-planning-agents/03-CONTEXT.md (D-14 through D-17 decisions on playbook design)
    - .planning/phases/03-truth-surface-and-planning-agents/03-RESEARCH.md (playbook directory setup, @ reference mechanism)
    - .claude/skills/_shared/rules.md (existing shared rules pattern — playbooks follow same approach)
  </read_first>
  <action>
Per D-14 (principle migration from ECL, no direct copy), D-15 (.detent/playbooks/ location), D-16 (@ reference by agents), D-17 (fixed default content).

Create `.detent/playbooks/` directory and three files:

**stage-playbook.md** — Quality standards for each planning stage:
```markdown
# Stage Playbook

Quality standards for each stage of the Detent adversarial planning pipeline.

## General Principles

- Every claim must be grounded in the truth surface (frozen decisions and constraint ledger)
- Identify contradictions explicitly — do not paper over disagreements
- Each stage produces a concrete artifact, not just analysis
- Time-box your work: respect maxTurns and produce output before running out

## Stage D: Critique

- Attack at least one requirement — if all requirements look solid, challenge their completeness
- Check every requirement against frozen decisions for contradictions
- Propose new constraints via `truth-propose` when gaps are found
- Output must be structured: requirement ID, challenge, evidence, proposed constraint

## Stage G: Red/Blue Adversarial

- G-Red: Your job is to ATTACK, not agree. Find the weakest point and exploit it.
- G-Blue: Your job is to DEFEND with evidence. If an attack is valid, acknowledge it and propose mitigation — do not deny valid criticism.
- Both: Reference specific frozen decisions and constraint ledger entries as evidence
- Both: Mark entries as challenged via `truth-update` to satisfy the maturity gate

## Stage H: Review

- Binary verdict only: approved or rejected. No "approved with reservations."
- Rejection must specify exactly which stage to return to (D or G) and why
- Approval means you guarantee the plan respects all frozen decisions
- Check the handoff quality bar before approving

## Stage J: Compile

- The handoff document must be self-contained — a Coder agent reads only this document
- Every implementation unit must have: files to create/modify, acceptance criteria, applicable frozen constraints
- Ambiguity is a defect. If you are unsure, state the assumption explicitly.
- Order units by dependency — unit N should not depend on unit N+1
```

**subagent-protocol.md** — Rules for agent behavior within the pipeline:
```markdown
# Subagent Protocol

Rules governing agent behavior within the Detent adversarial planning pipeline.

## Identity

- You are a subagent invoked by the /detent:plan orchestrator skill
- You have a specific role (D-Critique, G-Red, G-Blue, H-Review, or J-Compile)
- You do NOT control the pipeline — the orchestrator decides what runs next

## Truth Surface Rules

- NEVER use the Write tool on files inside `.detent/truth-surface/` — all mutations go through the CLI
- To propose a new constraint: `node ./detent-tools.cjs truth-propose --dir . --id <ID> --file <file> --source-agent <your-name> --rationale "<text>"`
- To mark an entry as challenged: `node ./detent-tools.cjs truth-update --dir . --id <ID> --file <file> --challenged-by <your-name>`
- To read truth surface: `node ./detent-tools.cjs truth-read --dir . --file <file>`
- FROZEN entries are immutable — do not attempt to modify them. Work within their constraints.

## Output Rules

- Write your output to the designated file in `.detent/plan/` (specified in your role instructions)
- Your output must be complete and self-contained — the next agent reads only your output file and the truth surface
- Use structured format with clear sections, not stream-of-consciousness prose

## Constraints

- Respect your tool permissions — do not attempt to use tools not granted to you
- Respect your maxTurns limit — produce output before running out of turns
- If you encounter an error from detent-tools.cjs, report it clearly and stop — do not attempt manual recovery
```

**handoff-quality-bar.md** — Acceptance criteria for the code handoff document:
```markdown
# Handoff Quality Bar

Minimum quality standards for the J-Compile code handoff document (.detent/plan/handoff.md).

## Required Sections

The handoff document MUST contain all of these sections:

1. **Summary** — One paragraph describing what is being built and why
2. **Frozen Constraints** — List of all FROZEN decisions that apply, with their IDs
3. **Implementation Units** — Ordered list of coding units

## Implementation Unit Requirements

Each unit MUST specify:

- **Unit ID** — Unique identifier (e.g., UNIT-01)
- **Description** — What this unit implements
- **Files** — Exact file paths to create or modify
- **Dependencies** — Which other units must complete first (or "none")
- **Acceptance Criteria** — Testable conditions that prove the unit is correct
- **Frozen Constraints** — Which frozen decisions apply to this unit (by ID)

## Quality Checks

Before finalizing the handoff, verify:

- [ ] Every frozen decision is referenced by at least one unit
- [ ] No unit depends on a later unit (ordering is valid)
- [ ] Every file path is specific (no "relevant files" or "etc.")
- [ ] Every acceptance criterion is testable (can be checked with a command or assertion)
- [ ] No ambiguous language ("should", "might", "could consider") — use "must", "will", "exactly"

## Rejection Triggers

H-Review SHOULD reject the plan if:

- Any frozen decision is contradicted by the proposed implementation
- A unit's acceptance criteria are vague or untestable
- Units have circular dependencies
- The handoff references files or APIs that don't exist and aren't being created
```
  </action>
  <verify>
    <automated>test -f .detent/playbooks/stage-playbook.md && test -f .detent/playbooks/subagent-protocol.md && test -f .detent/playbooks/handoff-quality-bar.md && echo "ALL PLAYBOOKS EXIST" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - .detent/playbooks/stage-playbook.md contains "# Stage Playbook" and "Stage D: Critique" and "Stage G:" and "Stage H:" and "Stage J:"
    - .detent/playbooks/subagent-protocol.md contains "# Subagent Protocol" and "truth-propose" and "truth-update" and "FROZEN entries are immutable"
    - .detent/playbooks/handoff-quality-bar.md contains "# Handoff Quality Bar" and "Implementation Unit Requirements" and "Rejection Triggers"
    - .detent/playbooks/subagent-protocol.md contains "node ./detent-tools.cjs"
  </acceptance_criteria>
  <done>Three playbook files exist with quality standards covering stage behavior, subagent protocol (including truth surface CLI commands), and handoff acceptance criteria. Agents can reference these via @ paths.</done>
</task>

</tasks>

<verification>
- All 5 agent files exist in .claude/agents/ with valid YAML frontmatter
- All 3 playbook files exist in .detent/playbooks/
- Agent tool permissions follow minimum privilege: D-Critique and G-Red have Read+Bash only; G-Blue and H-Review have Read+Bash+Write; J-Compile has Read+Write
- H-Review template explicitly shows JSON verdict format
- All agents reference at least one playbook via @ path
</verification>

<success_criteria>
- 5 agent template files in .claude/agents/ with correct frontmatter and role-specific instructions
- 3 playbook files in .detent/playbooks/ with quality standards
- Each agent specifies its output file convention in .detent/plan/
- H-Review verdict format is machine-parseable JSON with verdict, reentry_stage, reason fields
- No agent has Write tool access to .detent/truth-surface/ (all truth surface mutations through CLI)
</success_criteria>

<output>
After completion, create `.planning/phases/03-truth-surface-and-planning-agents/03-02-SUMMARY.md`
</output>
